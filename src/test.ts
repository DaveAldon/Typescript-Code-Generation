const hello: string = 'hello';
const world: string = 'world';
console.log(hello, world);
if (hello === 'hello') {
    if (hello === 'hello') {
        const result = world;
        const result2 = result;
    }
    const result = world;
    const result2 = result;
}
